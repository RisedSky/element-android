<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/discord_main_panel_bg">

    <!-- Search Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="16dp"
        android:background="@color/discord_channel_header_bg">

        <EditText
            android:id="@+id/discord_search_input"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Search"
            android:textColor="@color/discord_text_primary"
            android:textColorHint="@color/discord_text_muted"
            android:background="@null"
            android:textSize="16sp"
            android:maxLines="1"
            android:imeOptions="actionSearch" />

        <ImageView
            android:id="@+id/discord_search_close"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_close_24dp"
            android:background="?selectableItemBackgroundBorderless"
            app:tint="@color/discord_text_secondary" />

    </LinearLayout>

    <!-- Search Results -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/discord_search_results"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/discord_main_panel_bg"
        android:clipToPadding="false"
        android:scrollbars="none" />

</LinearLayout>